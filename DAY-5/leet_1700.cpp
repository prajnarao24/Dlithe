//number of students unable to eat lunch
#include<stdio.h>
#include<queue>
#include<vector>
using namespace std;
//class solution{
   /* public:
    int countStudents(queue<int>students,queue<int>sandwiches){
        int cnt[2]={0,0};
        for(auto p:ppl){
        cnt[p]++;
                }
        for(auto f:food){
            if9f=1&&cnt[f]!=0){
                cnt[f]--;
            }}
       /* int zero_students=0;
        int ones_students=0;
    
    for(int i=0;i<students.size();i++){
        if(students[i]==0){
            zero_students++;
        }
        else{
            ones_students++;
        }
    }
    for(int i=0;i<sandwiches.size();i++){
        if(sandwiches[i]==0){
            return ones_students;
        }
            zero_students--;
    }
        else{
        if(ones_students==0){
            return zero_students;
        }
        ones_students--;
        }
}
    return 0;
}};
int main()
*/